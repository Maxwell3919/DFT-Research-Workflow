# Scientific review — structure candidate construction

The page was reviewed against the committed `structure-defect-interface-candidates` case and its deterministic clean-directory reruns. ASE performs the site deletion and layer construction; pymatgen independently materializes the exported periodic structures; the page companion verifies manifest hashes and recorded metrics without rewriting evidence.

The evidence supports only the declared transformations: a 64-to-63 atom Silicon one-site-deletion candidate with no encoded charge state and a 16-atom imposed-common-cell graphene/h-BN bilayer candidate. The imposed `0.0%` mismatch is a construction constraint. The `3.35 Å` separation is a demonstration input. Neither value is a recommendation or a result of relaxation.

No electronic-structure solver, geometry optimizer, supercell-size series, registry search, strain study, vacuum study, or energetic comparison was run. Execution success is not defect convergence, interface convergence, physical stability, or scientific validation. The projection is generated from the exported structures and is secondary to the selectable files and terminal checks.

The page is reviewed within this construction-only boundary. It must not be cited for vacancy formation energy, charge-state stability, interface adhesion, stable registry, band alignment, or any material conclusion.
