# Graphene vacuum and adsorbate construction

Run with the versions in `environment.txt`, for example `PYTHON=python3 bash run.sh`, then `bash check.sh`. This is an executed ASE construction of a finite-z graphene model and an H candidate; it is not a relaxation or adsorption-energy calculation.
