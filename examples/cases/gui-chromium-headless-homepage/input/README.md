# Input boundary

Chromium receives the versioned local document at
`../source/walkthrough.html` directly as a `file://` URL.  There is no separate
calculation input deck for this browser-rendering case.
