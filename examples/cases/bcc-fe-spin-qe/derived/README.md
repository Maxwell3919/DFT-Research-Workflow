# Derived results pending

After all declared outputs are present, `parse.py` produces a strict parsed
summary, a mesh-screen result, and an original PNG of the FM/NM energy
comparison.  No derived magnetic result is committed before that execution.
