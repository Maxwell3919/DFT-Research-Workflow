# No data figure for this retrieval case

This case deliberately contains no plot or structure rendering. Its evidence is
the selectable CIF, captured HTTP headers, and deterministic parser output.
A figure would not establish that the database download occurred, that the
structure is scientifically valid, or that a calculation was run.
