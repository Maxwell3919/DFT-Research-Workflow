This directory is intentionally empty of QE results until `run.sh` is executed
inside an existing allocation.  The run retains only small text outputs and
`graphene.bands.dat`; no pseudo payload, charge density, wavefunction, restart
directory, or scheduler metadata is committed.
