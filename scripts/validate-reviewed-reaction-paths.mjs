import{readFile}from'node:fs/promises';const r=new URL('../',import.meta.url),a=await readFile(new URL('src/content/topics/reaction-paths-and-transition-states.md',r),'utf8'),v=await readFile(new URL('docs/reviews/2026-08-04-reaction-paths-and-transition-states.md',r),'utf8'),m=JSON.parse(await readFile(new URL('sources/reviewed-links.json',r),'utf8')),u=['https://doi.org/10.1063/1.1329672','https://doi.org/10.1063/1.1323224','https://www.quantum-espresso.org/Doc/INPUT_NEB.html','https://www.quantum-espresso.org/Doc/user_guide_PDF/neb_user_guide.pdf','https://docs.ase-lib.org/ase/neb.html'],p=['Endpoints define the question before a path can answer it','A nudged elastic band is a path optimization, not an ordinary relaxation','Climbing images refine a candidate saddle, but must still be checked','cannot establish an experimental rate'];let e=[];for(const x of p)if(!a.includes(x))e.push(x);const x=m.topics.find(t=>t.topic_slug==='reaction-paths-and-transition-states');if(!x||JSON.stringify(x.links.map(l=>l.url))!==JSON.stringify(u))e.push('manifest');for(const x of u)if(!a.includes(x)||!v.includes(x))e.push(x);if(!v.includes('Execution success is not an NEB calculation'))e.push('review boundary');if(e.length){console.error(e.join('\n'));process.exit(1)}console.log('Reviewed reaction-paths topic valid: endpoint, NEB, saddle, and rate boundaries.');
