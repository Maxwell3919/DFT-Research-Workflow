import{readFile,readdir}from'node:fs/promises';const root=new URL('../',import.meta.url),errors=[],slug=/^[a-z0-9]+(?:-[a-z0-9]+)*$/,registry=JSON.parse(await readFile(new URL('workflow/tools.json',root),'utf8')),workflow=JSON.parse(await readFile(new URL('workflow/topics.json',root),'utf8')),topics=new Set(workflow.sections.flatMap(s=>s.groups.flatMap(g=>g.topics.map(t=>t.slug)))),guideTools=new Set();for(const file of(await readdir(new URL('src/content/practical-guides/',root))).filter(f=>f.endsWith('.md'))){const text=await readFile(new URL(`src/content/practical-guides/${file}`,root),'utf8'),block=text.match(/tools:\n((?:\s+- .+\n?)+)/)?.[1]??'';for(const m of block.matchAll(/^\s+-\s+([a-z0-9-]+)$/gm))guideTools.add(m[1])}if(registry.schema_version!==1||!String(registry.authority).includes('secondary tool'))errors.push('registry authority');if(!String(registry.scope_note).includes('not research operations'))errors.push('scope boundary');const slugs=new Set(),names=new Set(),urls=new Set();for(const tool of registry.tools??[]){if(!slug.test(tool.slug)||slugs.has(tool.slug))errors.push(`${tool.slug}: slug`);slugs.add(tool.slug);if(!tool.name||names.has(tool.name))errors.push(`${tool.slug}: name`);names.add(tool.name);if(!['open-source','restricted-license'].includes(tool.access))errors.push(`${tool.slug}: access`);for(const key of['homepage','documentation'])if(!String(tool[key]).startsWith('https://'))errors.push(`${tool.slug}: ${key}`);for(const url of[tool.homepage,tool.documentation,tool.source_repository].filter(Boolean))urls.add(url);for(const topic of tool.topics??[])if(!topics.has(topic))errors.push(`${tool.slug}: unknown topic ${topic}`)}for(const tool of guideTools)if(!slugs.has(tool))errors.push(`practical tool missing: ${tool}`);if(registry.tools.length!==14||urls.size!==41)errors.push(`expected 14/41, found ${registry.tools.length}/${urls.size}`);if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log(`Tool registry valid: 14 identity-reviewed tools, 41 official URLs, resolved A-E topics, and complete practical-tool coverage.`);
