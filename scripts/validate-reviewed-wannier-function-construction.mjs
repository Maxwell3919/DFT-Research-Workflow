import{readFile}from'node:fs/promises';const r=new URL('../',import.meta.url),a=await readFile(new URL('src/content/topics/wannier-function-construction.md',r),'utf8'),v=await readFile(new URL('docs/reviews/2026-08-04-wannier-function-construction.md',r),'utf8'),m=JSON.parse(await readFile(new URL('sources/reviewed-links.json',r),'utf8')),u=['https://doi.org/10.1103/PhysRevB.56.12847','https://doi.org/10.1103/PhysRevB.65.035109','https://wannier90.readthedocs.io/en/latest/user_guide/wannier90/methodology/','https://wannier90.readthedocs.io/en/latest/user_guide/wannier90/notes_interpolations/','https://wannier90.readthedocs.io/en/latest/tutorials/tutorial_3/'],p=['A gauge choice over a periodic subspace','Isolated bands and disentangled subspaces are different problems','From overlaps to an interpolated Hamiltonian','This topic does not establish a unique'];let e=[];for(const x of p)if(!a.includes(x))e.push(x);const x=m.topics.find(t=>t.topic_slug==='wannier-function-construction');if(!x||JSON.stringify(x.links.map(l=>l.url))!==JSON.stringify(u))e.push('manifest');for(const x of u)if(!a.includes(x)||!v.includes(x))e.push(x);if(!v.includes('No companion execution or material image'))e.push('review boundary');if(e.length){console.error(e.join('\n'));process.exit(1)}console.log('Reviewed Wannier topic valid: gauge, disentanglement, interpolation, and claim boundaries.');
