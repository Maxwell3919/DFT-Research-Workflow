import{readFile}from'node:fs/promises';const r=new URL('../',import.meta.url),a=await readFile(new URL('src/content/topics/quantum-transport.md',r),'utf8'),v=await readFile(new URL('docs/reviews/2026-08-04-quantum-transport.md',r),'utf8'),m=JSON.parse(await readFile(new URL('sources/reviewed-links.json',r),'utf8')),u=['https://doi.org/10.1147/rd.13.0223','https://research.ibm.com/publications/four-terminal-phase-coherent-conductance','https://arxiv.org/abs/cond-mat/0110650','https://arxiv.org/abs/1607.04464','https://docs.siesta-project.org/projects/siesta/en/5.4/reference/siesta.html#transiesta'],p=['The device is defined by its boundaries','Green functions turn contacts into energy-dependent boundary conditions','Landauer current is a reservoir imbalance filtered by transmission','Self-consistency and many-body content define what the result means','Converge the open system and the observable','Preserve a reusable device record'];let e=[];for(const x of p)if(!a.includes(x))e.push(x);const x=m.topics.find(t=>t.topic_slug==='quantum-transport');if(!x||JSON.stringify(x.links.map(l=>l.url))!==JSON.stringify(u))e.push('manifest');for(const x of u)if(!a.includes(x)||!v.includes(x))e.push(x);for(const x of['bulk conductivity from device conductance','zero-bias transmission from nonlinear current','SCF convergence from observable convergence','does not claim that an electrode'])if(!v.includes(x)&&!a.includes(x))e.push(x);if(e.length){console.error(e.join('\n'));process.exit(1)}console.log('Reviewed quantum-transport topic valid: open-boundary, NEGF, Landauer, finite-bias, convergence, and claim boundaries.');
